<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>SuperSite</title>
</head>

<body>
    <ul>
        {{range .}}
        <li>{{.}}</li>
        {{end}}
    </ul>
    <ul>
        {{range $key, $val := .}}
        <li>{{$key}} - {{$val}}</li>
        {{end}}
    </ul>
</body>

</html>